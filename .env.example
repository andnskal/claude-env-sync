# ───────────────────────────────────────────────────
# 이 파일은 자동 생성됨 — 실제 값은 import.ps1 실행 시 입력
# 절대 .env에 실제 값을 직접 적어 커밋하지 말 것
# ───────────────────────────────────────────────────

# 감지된 비밀 키가 없습니다.
